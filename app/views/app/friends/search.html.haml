= render :partial => "app/friends/sort_form", :locals => { :sort_url => sort_app_user_account_friends_path(@account), :search_url => search_app_user_account_friends_path(@account), :statistics_url => friends_app_user_account_statistics_network_path(@account) }

= content_area do
  %h3
    My Friends
    .grey.small
      Search results for
      %em.strong= params[:q]
  
  = render_list(:friends, @friends)
